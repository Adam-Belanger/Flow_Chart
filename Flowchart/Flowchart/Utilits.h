#ifndef UTILITES_H
#define UTILITES_H
#pragma once
#include <stdio.h>
void cleanBuffer();
char getChar();
int getInt();
void loadStringToMem(char* format, char* location);
void clear();
void title();
#endif